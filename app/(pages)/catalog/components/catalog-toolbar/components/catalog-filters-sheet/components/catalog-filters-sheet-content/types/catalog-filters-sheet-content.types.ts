import type { CatalogFiltersSheetProps } from "../../../types/catalog-filters-sheet.types";

/** The sheet stays mounted while closed, so the content needs `isOpen` to pause its count query. */
export type CatalogFiltersSheetContentProps = CatalogFiltersSheetProps;
